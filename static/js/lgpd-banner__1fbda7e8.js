(function(){const PREFS_KEY='bm_lgpd_prefs';const LEGACY_KEY='bm_lgpd';const TTL_DAYS=365;function readCookie(name){const prefix=name+'=';const parts=document.cookie.split(';');for(let i=0;i<parts.length;i++){const c=parts[i].trim();if(c.startsWith(prefix))return decodeURIComponent(c.slice(prefix.length));}
return null;}
function writePrefs(prefs){const d=new Date();d.setTime(d.getTime()+TTL_DAYS*24*60*60*1000);const value=encodeURIComponent(JSON.stringify(prefs));const isSecure=location.protocol==='https:';const attrs=['expires='+d.toUTCString(),'path=/','samesite=lax'];if(isSecure)attrs.push('secure');document.cookie=PREFS_KEY+'='+value+'; '+attrs.join('; ');}
function alreadyConsented(){return readCookie(PREFS_KEY)!==null||readCookie(LEGACY_KEY)!==null;}
function init(){if(alreadyConsented())return;const banner=document.getElementById('bm-lgpd-banner');if(!banner)return;banner.hidden=false;const form=document.getElementById('bm-lgpd-form');if(!form)return;form.addEventListener('click',function(ev){const target=ev.target;if(!target||target.tagName!=='BUTTON')return;const action=target.getAttribute('data-action');if(!action)return;let prefs;if(action==='accept-all'){prefs={essential:1,analytics:1,marketing:1};}else if(action==='reject-all'){prefs={essential:1,analytics:0,marketing:0};}else if(action==='accept-selected'){const analytics=form.querySelector('input[name="analytics"]');const marketing=form.querySelector('input[name="marketing"]');prefs={essential:1,analytics:analytics&&analytics.checked?1:0,marketing:marketing&&marketing.checked?1:0,};}else{return;}
writePrefs(prefs);banner.hidden=true;});}
if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',init);}else{init();}})();